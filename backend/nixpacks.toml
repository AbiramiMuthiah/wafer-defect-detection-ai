[phases.setup]
nixPkgs = ["python311", "libxcb", "xorg.libXext", "xorg.libX11", "xorg.libXrender", "xorg.libXtst", "xorg.libXi", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"